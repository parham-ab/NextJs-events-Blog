import React from "react";

const NotFoundResult = ({ title }) => {
  return (
    <div className="d-flex justify-content-center bg-warning">
      <h1>{title}</h1>
    </div>
  );
};

export default NotFoundResult;
